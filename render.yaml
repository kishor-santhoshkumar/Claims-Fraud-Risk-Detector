services:
  - type: web
    name: claims-fraud-detector
    env: docker
    plan: standard          # 2 GB RAM — needed for ML stack at startup
    healthCheckPath: /api/
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GROQ_API_KEYS
        sync: false         # set this in the Render dashboard
